# Required for /api/analyze and /api/analyze/batch. Create at https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=

# Required for signing analysis results and /api/verify. Use a long random string (e.g. 32+ chars).
SIGNING_SECRET=
